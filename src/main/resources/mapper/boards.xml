<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.red.domain.boards.BoardsDao">

	<update id="updateByUsersId">
		UPDATE boards SET usersId = null WHERE usersId =#{usersId}
	</update>
	
	<select id="paging"
		resultType="site.metacoding.red.web.dto.response.boards.PagingDto">
		SELECT a.totalCount,
		a.totalPage,
		a.currentPage,
		case when a.currentPage = 0 then 1 else 0 end as first,
		case when a.currentPage = totalPage-1 then 1 else 0 end as last
		FROM
		(
		select count(*) totalCount, 
		ceil(count(*)/3) totalPage,
		#{page} currentPage,
		0 first,
		0 last
		FROM boards
		<if test="keyword != null">
			WHERE title like CONCAT('%',#{keyword},'%')
		</if>
		) a
	</select>

	<insert id="insert">
		INSERT INTO boards(title, content, usersId,
		createdAt)
		VALUES(#{title}, #{content}, #{usersId},
		now())
	</insert>

	<select id="findById"
		resultType="site.metacoding.red.domain.boards.Boards">
		SELECT * FROM boards
		WHERE id=#{id}
	</select>

	<select id="findAll"
		resultType="site.metacoding.red.web.dto.response.boards.PagingDto">
		SELECT b.id, b.title, u.username
		FROM boards b
		INNER JOIN
		users u
		ON
		b.usersId = u.id
		<if test="keyword != null">
			WHERE title like CONCAT('%',#{keyword},'%')
		</if>
		ORDER BY b.id DESC
		OFFSET #{startNum} ROWS FETCH FIRST 3 ROWS ONLY
	</select>

	<update id="update">
		UPDATE boards SET title = #{title}, 
		content = #{content} WHERE id = #{id}
	</update>
	
	<delete id="deleteById">
		DELETE FROM boards WHERE id = #{id}
	</delete>

</mapper>


